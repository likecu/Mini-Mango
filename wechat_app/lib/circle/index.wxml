<view style="box-sizing:null;" class="wux-class {{ classes.wrap }}" style="{{ style }}">
    <canvas style="{{ style }}" canvas-id="circle" />
    <view style="box-sizing:null;" class="{{ classes.inner }}">
    	<slot></slot>
    </view>
</view>