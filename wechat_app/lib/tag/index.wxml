<view style="box-sizing:null;" class="wux-class {{ classes.wrap }} {{ className }}" hover-class="{{ classes.hover }}" style="{{ tagStyle }}" bindtap="onClick" wx:if="{{ tagVisible }}">
    <slot></slot>
    <view style="box-sizing:null;" class="{{ classes.icon }}" catchtap="onClose" wx:if="{{ closable }}"></view>
</view>