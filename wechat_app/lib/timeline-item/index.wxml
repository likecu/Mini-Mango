<view style="box-sizing:null;" class="wux-class {{ classes.wrap }} {{ className }}">
    <view style="box-sizing:null;" class="{{ classes.tail }}"></view>
    <view style="box-sizing:null;" class="{{ classes.dot }}" style="{{ extStyle }}">
    	<slot wx:if="{{ custom }}" name="dot"></slot>
    </view>
    <view style="box-sizing:null;" class="{{ classes.content }}">
    	<text>{{ content }}</text>
        <slot></slot>
    </view>
</view>
