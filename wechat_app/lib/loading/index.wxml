<wux-backdrop id="wux-backdrop" transparent="{{ transparent }}" wx:if="{{ mask }}" />
<wux-animation-group wux-class="{{ classes.wrap }}" in="{{ in }}" classNames="{{ classNames }}">
    <view style="box-sizing:null;" class="{{ classes.content }}">
        <view style="box-sizing:null;" class="{{ classes.icon }}"></view>
        <view style="box-sizing:null;" class="{{ classes.text }}">{{ text }}</view>
    </view>
</wux-animation-group>
